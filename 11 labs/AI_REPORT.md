# AI_REPORT — Lab 11 (Lab 11.1 + Lab 11.2)

## Student
- Name: Радмир Абраев
- Student ID: <вставь свой ID>
- Date: 26.03.2026

## 1. Использованные AI-инструменты

- Инструмент: Perplexity, модель Perplexity (GPT-5.4)
- Формат работы:
  - Загрузка условия лабораторной работы (PDF).
  - Запрос на генерацию структуры проекта, кода для всех файлов и общего AI_REPORT.
  - Уточнение требований: единый AI_REPORT для Lab 11.1 и Lab 11.2, имена файлов, структура папок.

## 2. Какие подсказки (prompts) использовались

Примеры ключевых запросов:
- «Сделай все задания. Сделай один общий AI_REPORT.md на оба задания. Не по отдельности. Дай все файлы с кодом и напиши какие это именно файлы.»
- Внутри сессии: описание требований к Lab 11.1 и Lab 11.2, чтобы ИИ сгенерировал:
  - PromiseHttpClient, retry, AsyncQueue, RateLimiter.
  - DataFetcher, AsyncPipeline и утилиты (mapParallel, reduceAsync и др.).
  - CancellableFetcher, TaskRunner.
  - Файлы index.html и README для каждой задачи.
  - Общий AI_REPORT.

## 3. Как я модифицировал и проверял код

- Структура:
  - Принял предложенную структуру `Lab_11/task1/` и `Lab_11/task2/` из условия.
  - Убедился, что имена файлов совпадают с требованиями: `PromiseHttpClient.js`, `retry.js`, `AsyncQueue.js`, `DataFetcher.js`, `Pipeline.js`, `CancellableFetcher.js`, `index.html`, `README.md`.

## 4. Чему я научился

- Асинхронные паттерны:
  - Разница между последовательным и параллельным выполнением (`await` по очереди vs `Promise.all`).
  - Использование `Promise.allSettled` для частичных ошибок и сборки агрегированного результата.
  - Реализация retry с экспоненциальным backoff и настраиваемыми статусами/типами ошибок.

- Архитектура:
  - Вынесение HTTP-клиента в отдельный класс с единым форматом ошибок (`HttpError`).
  - Построение асинхронного pipeline, где каждая стадия отчётливо отделена и может возвращать как данные, так и дополнительный контекст.
  - Использование очереди и лимитера для контроля количества параллельных задач.

- Ошибки и отмена:
  - Важность явной обработки ошибок во всех async-функциях (`try/catch`, `.catch`, понятные сообщения).
  - Использование `AbortController` для отмены запросов `fetch` и паттерн `TaskRunner` для отмены произвольных async-задач.
  - Практика: всегда чистить ресурсы (удалять контроллеры из `Map`, сбрасывать таймеры, очищать очереди).
